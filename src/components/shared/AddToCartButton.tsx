"use client";

import React from "react";
import { Button } from "../ui/button";

const AddToCartButton = () => {
  return (
    <Button type="button" className="bg-primary text-foreground">
      Add
    </Button>
  );
};

export default AddToCartButton;
